dist: bionic

language: go

go:
  - 1.15

before_install:
  - npm install -g snyk
  - snyk test
  - go get -t -v ./...

script:
  - make
  - ls
  - go test -race -coverprofile=coverage.txt -covermode=atomic ./...
